<nav class="navbar navbar-light bg-light mb-4" style=" background-color: #e3f2fd; width: 100%; height: 70px">
    <div class="container">
        <a class="navbar-brand " href="{{ path('app_home') }}" >
            <img src="/images/ColisTN.png"  id="header_not_logged" height="70px;" width="150px;" style="text-align: center" >
        </a>
        {%  if is_granted('ROLE_ADMIN') %}
            <a class="nav-link text-black-50" href="{{ path('app_user') }}">Users</a>
        {% endif %}
        <a class="nav-link text-black-50" href="{{ path('app_trajet') }}">Trajets</a>
        <a class="nav-link text-black-50" href="{{ path('app_annonce') }}">Annonces</a>
        <a class="nav-link text-black-50" href="{{ path('app_blog') }}">Blog</a>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a class="nav-link text-black-50" href=#>Account </a>
            <a class="nav-link text-black-50" href="{{ path('app_logout') }}">Se deconnecter</a>
        {% else %}
            <a class="nav-link text-black-50" href="{{ path('app_login') }}">Se connecter</a>
            <a href="{{ path('app_register') }}" class="nav-link text-black-50">Inscription</a>
        {% endif %}
    </div>

</nav>