<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Spartan&display=swap">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.1/css/all.min.css" integrity="sha256-mmgLkCYLUQbXn0B1SRqzHar6dCnv9oZFPEC1g1cwlkk=" crossorigin="anonymous" />
        <link rel="stylesheet" href="/css/app.css">
    {% endblock %}
</head>
<body class="flex flex-col justify-between items-center min-h-screen">
<main role="main" class="my-6">
<nav class="navbar navbar-light bg-light" style="height: 80px;">
    <a class="navbar-brand" href="{{ path('app_home') }}">
        <i style="color: #444; font-size: 2rem;" class="pb-1 fad fa-cauldron"></i>
        <p class="pl-2 d-inline font-weight-bold" style="color: #444;">COLIS.TN </p>
    </a>
                {%  if is_granted('ROLE_ADMIN') %}
                    <a class="nav-link" href="{{ path('app_user') }}">Users</a>
                {% endif %}
                <a class="nav-link" href="{{ path('app_trajet') }}">Trajets</a>
                <a class="nav-link" href="{{ path('app_annonce') }}">Annonces</a>
                <a class="nav-link" href="{{ path('app_blog') }}">Blog</a>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <a class="nav-link text-black-50" href="{{ path('app_logout') }}">Se deconnecter</a>
        {% else %}
            <a class="nav-link text-black-50" href="{{ path('app_login') }}">Se connecter</a>
            <a href="{{ path('app_register') }}" class="btn btn-dark">Inscription</a>
        {% endif %}
</nav>


    {% block body %}{% endblock %}

</main>

<footer class="mt-5 p-3 text-center my-6" style="width: 100%; height: 50px; position: fixed; bottom:0; left:0 ">
    Made with <i style="color: red;" class="fa fa-heart"></i> by <a style="color: #444; " >HADDAD Souha</a>
    &copy; Copyright {{ 'now'|date('Y') }}
</footer>
</body>
</html>
